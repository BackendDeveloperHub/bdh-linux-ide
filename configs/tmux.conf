# =========================================================
# 🔄 WINDOW NAVIGATION (Laptop & Mobile Support)
# =========================================================

# 💻 Laptop-க்காக: Shift + Arrow keys (S = Shift)
bind -n S-Left select-pane -L
bind -n S-Right select-pane -R
bind -n S-Up select-pane -U
bind -n S-Down select-pane -D

# 📱 Mobile (Termux)-க்காக: Alt + Arrow keys (M = Meta/Alt)
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# =========================================================
# 🛑 KILL SESSION
# =========================================================
# Fn + 4 (வேலை செய்ததால் அதை அப்படியே வைத்துக்கொள்ளலாம்)
bind -n F4 kill-session -t archide
